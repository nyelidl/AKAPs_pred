# AKAPSpred v5.1 + ML v2 — pinned for reproducible model unpickling on Streamlit Cloud.
# The ML bundle was pickled with xgboost 3.2.0; loosely-pinned deps let Cloud install
# an incompatible build and the model then fails to unpickle ("ML unavailable").
# If you trained locally with different versions, set these to YOUR `pip freeze` values
# for scikit-learn / xgboost / numpy / scipy / joblib (the env that loads the .joblib).
numpy>=1.26,<3
scipy>=1.11
pandas>=2.0
openpyxl>=3.1
biopython>=1.81
scikit-learn==1.8.0
xgboost==3.2.0
joblib>=1.3
streamlit>=1.30
plotly>=5.18
requests>=2.31
